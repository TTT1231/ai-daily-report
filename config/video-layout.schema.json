{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "./video-layout.schema.json",
  "title": "AI Daily Report Video Layout",
  "description": "项目级固定视频布局配置。它由人工维护，供 RSS 容量计算、数据校验和 Remotion 渲染共同读取，不会随单次日报生成而自动变化。",
  "type": "object",
  "required": ["$schema", "width", "height", "navigation"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "const": "./video-layout.schema.json",
      "description": "供编辑器识别本文件对应的 JSON Schema，并提供字段说明与校验。"
    },
    "width": {
      "type": "integer",
      "minimum": 1,
      "description": "视频画布宽度，单位为像素。Remotion Composition 和导航容量计算共用此值。"
    },
    "height": {
      "type": "integer",
      "minimum": 1,
      "description": "视频画布高度，单位为像素。Remotion Composition 使用此值。"
    },
    "navigation": {
      "$ref": "#/$defs/navigation",
      "description": "顶部栏目导航与底部 Story 导航共用的宽度估算及响应式字号配置。"
    }
  },
  "$defs": {
    "navigation": {
      "type": "object",
      "required": [
        "maxTopCategories",
        "topComfortFillRatio",
        "minimumItemWidth",
        "edgeInset",
        "itemGap",
        "bottomWindowItems",
        "bottomInactiveFontSize",
        "bottomActiveFontSize",
        "bottomHorizontalPadding",
        "bottomActiveExtraGap",
        "bottomCounterWidth",
        "asciiWidthFactor",
        "itemChromeWidth",
        "layouts"
      ],
      "additionalProperties": false,
      "properties": {
        "maxTopCategories": {
          "type": "integer",
          "minimum": 1,
          "maximum": 5,
          "description": "正文栏目数量上限，不含概览和结语；即使像素宽度足够也不得增加栏目。同类 Story 连续编排。"
        },
        "topComfortFillRatio": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 1,
          "description": "顶部导航的可观赏性软阈值，占视频宽度的比例。超过该值但未越过画布时只提示偏密，不阻断；实际宽度超过画布才校验失败。"
        },
        "minimumItemWidth": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "每个导航标签的最小展示宽度，单位为像素。即使文字较短，标签也不会窄于此值。"
        },
        "edgeInset": {
          "type": "number",
          "minimum": 0,
          "description": "整条导航左右两侧各自保留的空白，单位为像素。"
        },
        "itemGap": {
          "type": "number",
          "minimum": 0,
          "description": "相邻导航标签之间的间距，单位为像素。"
        },
        "bottomWindowItems": {
          "type": "integer",
          "minimum": 3,
          "maximum": 7,
          "description": "底部 Story 导航单排显示的相邻标签数；渲染时以当前 Story 为中心滑动窗口，避免全量标题挤成多排。"
        },
        "bottomInactiveFontSize": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "底部导航窗口内非当前 Story 标签的字号，单位为像素。窗口内不使用 layouts 的响应式字号。"
        },
        "bottomActiveFontSize": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "底部导航窗口内当前 Story 标签的字号，单位为像素。宽度估算按窗口内最长标签取此字号计算最坏情况。"
        },
        "bottomHorizontalPadding": {
          "type": "number",
          "minimum": 0,
          "description": "底部导航每个标签左右两侧各自的内边距，单位为像素。"
        },
        "bottomActiveExtraGap": {
          "type": "number",
          "minimum": 0,
          "description": "底部导航当前 Story 标签内文字与序号胶囊之间的间距，单位为像素。"
        },
        "bottomCounterWidth": {
          "type": "number",
          "minimum": 0,
          "description": "底部导航当前 Story 标签尾部的序号胶囊（如“5 / 17”）的估算宽度，单位为像素，含其内边距与边框。"
        },
        "asciiWidthFactor": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 1,
          "description": "ASCII 字符相对中文全角字符的估算宽度比例，用于在渲染前计算英文和数字标签宽度。"
        },
        "itemChromeWidth": {
          "type": "number",
          "minimum": 0,
          "description": "除文字和水平内边距外，标签边框、装饰等占用的额外宽度，单位为像素。"
        },
        "layouts": {
          "type": "array",
          "minItems": 1,
          "description": "按 minItems 从大到小排列的响应式导航样式。系统使用第一个满足“当前标签数量大于等于 minItems”的配置；最后一项必须使用 minItems: 0 作为兜底。",
          "items": {
            "$ref": "#/$defs/navigationLayout"
          }
        }
      }
    },
    "navigationLayout": {
      "type": "object",
      "required": ["minItems", "fontSize", "horizontalPadding"],
      "additionalProperties": false,
      "properties": {
        "minItems": {
          "type": "integer",
          "minimum": 0,
          "description": "启用此样式所需的最少导航标签数量。layouts 必须按该值从大到小排列。"
        },
        "fontSize": {
          "type": "number",
          "exclusiveMinimum": 0,
          "description": "导航标签字号，单位为像素。"
        },
        "horizontalPadding": {
          "type": "number",
          "minimum": 0,
          "description": "导航标签左右两侧各自的内边距，单位为像素。"
        }
      }
    }
  }
}
